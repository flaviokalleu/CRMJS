import React from 'react';
import { Link } from 'react-router-dom'; // Utilize o Link para navegação interna

const Sidebar = () => {
    return (
        <nav className='sidebar'>
            
        </nav>
    );
};

export default Sidebar;
